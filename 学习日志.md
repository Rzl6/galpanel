# GALPANEL 学习日志：从 STM32 HAL 到 nRF52840、Zephyr 与 ZMK

## 教程使用方式

本文件同时承担三种用途：

1. **教程**：从环境安装开始，按顺序解释每个工具和概念；
2. **实操手册**：给出可以直接复制执行的命令和预期结果；
3. **实验记录**：记录 GALPANEL 实物在每一步出现的真实现象。

建议不要跳章。每完成一章，就把实际结果填写到对应的“实操记录”中。如果现象和预期不同，先停止进入下一章，保留错误信息并更新 `galpanel.log.md`。

当前进度：

```text
[完成] Windows 基础工具检查
[完成] SuperMini 首次连接与 UF2 Bootloader 识别
[完成] 本地 Git 仓库和 ZMK 工程骨架
[完成] 创建并同步 GitHub 远端仓库
[完成] 第一次 GitHub Actions 编译
[完成] 第一次刷入 galpanel.uf2
[完成] Windows USB 复合 HID 枚举验证
[待做] 实物按键、EC11 与双击 RST 测试
```

---

## 第 1 章：先理解我们需要哪些工具

### 1.1 初期必需工具

|工具|作用|当前状态|
|---|---|---|
|Git|保存版本、提交代码、推送 GitHub|已安装|
|GitHub 账号|保存远端仓库、运行云端编译|已使用 `Rzl6/galpanel`|
|浏览器|查看 GitHub Actions、下载 UF2|Windows 已具备|
|文件资源管理器|查看 NICENANO U 盘、复制 UF2|Windows 已具备|
|文本编辑器/VS Code|阅读和修改配置|建议安装 VS Code|

### 1.2 推荐但初期不强制

|工具|作用|什么时候需要|
|---|---|---|
|uv|安装和运行 ZMK CLI|已经安装；管理 Python 工具|
|ZMK CLI|管理配置仓库和键盘项目|当前可通过 `uvx zmk` 临时运行|
|WSL2|在 Windows 上提供 Linux 环境|以后做本地编译时使用|
|West/CMake/Ninja|本地编译 Zephyr/ZMK|GitHub Actions 路线暂不需要|
|nRF Connect Programmer|通过 J-Link/SWD 擦除和烧录|Bootloader 损坏时使用|
|nrfjprog|命令行 SWD 编程|底层调试和自动化时使用|
|J-Link 软件|连接 J-Link 调试器|救砖或底层调试时使用|

### 1.3 为什么暂时不用 Keil、West 和本地编译器

第一阶段只需要：

```text
修改配置 -> git push -> GitHub 编译 -> 下载 UF2 -> 拖入开发板
```

GitHub Actions 会在云端准备 Zephyr、ZMK、CMake、West 和 ARM 编译器。这样可以先把注意力放在 GPIO、键位和实物验证上。

等第一份 UF2 成功运行后，再安装 WSL2 和本地工具链。届时环境问题和硬件问题不会同时出现，学习压力会小很多。

---

## 第 2 章：Windows 环境配置

### 2.1 打开 PowerShell

在开始菜单中搜索 `PowerShell` 或打开 Windows Terminal。进入项目目录：

```powershell
cd 'F:\claude-test\galpanel'
```

确认当前位置：

```powershell
Get-Location
```

预期包含：

```text
F:\claude-test\galpanel
```

### 2.2 检查 Git

```powershell
git --version
git config --get user.name
git config --get user.email
```

本机实测：

```text
git version 2.49.0.windows.1
user.name  = renzuolin
user.email = 2747344498@qq.com
```

Git 用户名和邮箱只用于标记提交作者，不是登录密码。

### 2.3 检查 uv 和 ZMK CLI

```powershell
uv --version
uvx zmk --version
```

本机已经能够通过 `uvx` 临时运行 ZMK CLI，实测版本：

```text
ZMK CLI 0.4.1
```

`uvx` 会在隔离环境中临时安装和运行工具。如果希望以后直接使用 `zmk` 命令，可以执行：

```powershell
uv tool install zmk
```

安装后关闭并重新打开终端，再检查：

```powershell
zmk --version
```

这一步不是首次云端构建的硬性要求，可以稍后完成。

### 2.4 运行项目检查脚本

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\validate-project.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\check-environment.ps1
```

第一个脚本检查项目文件和关键配置；第二个脚本只读取环境，不会安装或删除软件。

当前实测结果：

- Git：已安装；
- uv：已安装；
- ZMK CLI：可通过 `uvx` 使用；
- West/CMake：未安装；
- nrfjprog/J-Link 命令行：未安装；
- 项目结构检查：通过。

这满足“先用 GitHub Actions 编译”的环境要求。

---

## 第 3 章：第一次连接 SuperMini nRF52840

### 3.1 当前真实现象

2026-07-26，将开发板通过 USB-C 插入电脑后，Windows 直接出现一个 U 盘：

```text
盘符：E:\
卷标：NICENANO
文件系统：FAT
类型：可移动磁盘
容量：约 32 MB
```

盘内文件：

```text
CURRENT.UF2
INDEX.HTM
INFO_UF2.TXT
```

`INFO_UF2.TXT` 的实际内容为：

```text
UF2 Bootloader 0.6.0
Model: nice!nano
Board-ID: nRF52840-nicenano
SoftDevice: S140 version 6.1.1
Date: Jun 19 2021
```

### 3.2 这说明了什么

这次识别结果确认了五件重要的事：

1. USB 数据线和开发板 USB 接口正常；
2. nRF52840 能够运行 Bootloader；
3. 开发板按 nice!nano 兼容路线配置；
4. `nice_nano//zmk` 是合理的首选 ZMK Board target；
5. 后续可以通过拖入 UF2 完成日常烧录。

### 3.3 为什么一插电脑就直接显示 U 盘

`NICENANO` 不是普通存储 U 盘，而是 UF2 Bootloader 模拟出来的烧录盘。

出现它表示 CPU 当前运行的是 Bootloader，而不是 GALPANEL 应用固件。常见原因包括：

- 板子还没有可正常启动的应用固件；
- 刚刚执行了双击 RST；
- 当前应用无效，Bootloader 留在等待烧录状态；
- 复位线路或按键持续让它进入 Bootloader。

对于刚拿到、还没刷 GALPANEL 固件的开发板，这个现象通常是正常且有利的，说明我们可以直接烧录 UF2。

### 3.4 当前不要做的操作

- 不要删除或修改 `INFO_UF2.TXT`、`INDEX.HTM`；
- 不要把 `.hex` 安装包或 Bootloader HEX 直接复制进 U 盘；
- 不要把 `CURRENT.UF2` 当成普通文件编辑；
- 暂时不要用 J-Link 擦除整片芯片；
- 在第一份 GALPANEL UF2 编译出来前，不需要向 U 盘写入任何内容。

`CURRENT.UF2` 是开发板当前 Flash 内容的 UF2 表示，可以用于备份分析，但不是我们日常需要编辑的源码。

### 3.5 如何再次进入 UF2 模式

以后刷入正常 ZMK 固件后，开发板不应该一直显示为 U 盘。需要重新烧录时：

```text
快速按两次 RST
-> NICENANO U 盘出现
-> 复制 galpanel.uf2
-> 开发板自动重启，U 盘消失
```

载板上的 RST 直接连接模块 RST，因此同样可以用于双击进入 Bootloader。

### 3.6 本章实操记录

```text
[通过] Windows 识别 E:\ NICENANO
[通过] 能读取 INFO_UF2.TXT
[通过] Bootloader 版本为 0.6.0
[通过] Model 为 nice!nano
[通过] Board-ID 为 nRF52840-nicenano
[通过] SoftDevice 为 S140 6.1.1
[未测试] 拔插后是否每次都自动进入 U 盘
[未测试] 快速双击 RST 能否稳定重新进入 U 盘
[未测试] 复制应用 UF2 后是否自动重启
```

---

## 第 4 章：建立 GitHub 云端编译环境

本地 Git 仓库已经创建，首次构建使用的提交为：

```text
3bb605e docs: expand setup tutorial and record UF2 bootloader
```

远端仓库已经配置为：

```text
https://github.com/Rzl6/galpanel
```

### 4.1 在 GitHub 创建空仓库

登录 GitHub，创建一个新仓库，推荐名称：

```text
galpanel
```

创建时注意：

- 可以选择 Public 或 Private；
- 不要让 GitHub 自动创建 README；
- 不要自动创建 `.gitignore`；
- 不要自动添加 License。

因为本地仓库已经有这些文件，远端应保持空白，避免第一次推送产生冲突。

### 4.2 添加远端地址

本项目实际使用：

```powershell
git remote add origin https://github.com/Rzl6/galpanel.git
git remote -v
```

预期看到：

```text
origin  https://github.com/你的用户名/galpanel.git (fetch)
origin  https://github.com/你的用户名/galpanel.git (push)
```

### 4.3 第一次推送

```powershell
git push -u origin main
```

如果需要登录，推荐使用浏览器弹出的 Git Credential Manager，不要把 GitHub 密码或 Token 写入项目文件。

### 4.4 查看自动构建

推送成功后：

1. 打开 GitHub 仓库；
2. 点击 `Actions`；
3. 打开 `Build ZMK firmware`；
4. 等待绿色对勾；
5. 登录 GitHub 后下载名为 `firmware` 的构建产物；
6. 解压后应得到 `galpanel.uf2`。

如果出现红色叉号，不要随意改代码。先复制完整错误日志，并在 `galpanel.log.md` 中记录构建链接和首个错误位置。

### 4.5 本项目第一次云端构建实录

2026-07-26 的第一次构建结果：

```text
仓库：Rzl6/galpanel
工作流：Build ZMK firmware
Run：#1
Run ID：30197044810
源提交：3bb605e515a98756e3b3c968b7e4dd3dfb4592be
目标：nice_nano//zmk + galpanel
总耗时：4 分 21 秒
结论：Success
产物名：firmware
```

构建页面：<https://github.com/Rzl6/galpanel/actions/runs/30197044810>

这次构建依次通过了 `West Update`、`West Build`、Kconfig、Devicetree 和产物归档。它证明当前仓库不仅“文件看起来正确”，而且已经能在干净的官方环境中编译成真实固件。

首次构建验证后，`config/west.yml` 将 ZMK 固定到：

```text
f84ef436cd8e88f9d1580a9b86bab2fae8d349c6
```

这类似于 STM32 工程固定 Cube/HAL 版本：以后上游 `main` 更新时，不会让同一份 GALPANEL 配置在不知情的情况下改变或突然编译失败。

固定版本后的验证过程也值得记录：Run #2 因 GitHub Runner 拉取 Docker 镜像失败而中止，编译器尚未启动；不修改代码重新触发 Run #3 后构建成功。Run #3 的 `galpanel.uf2` 与第一次刷入的文件 SHA-256 完全相同，说明这次固定上游版本没有改变固件内容，也不需要重复刷写。

```text
Run #3：https://github.com/Rzl6/galpanel/actions/runs/30197484437
源提交：c1b280df67bad59b3565b4831599b041adc16ccc
结论：Success
耗时：3 分 33 秒
UF2 SHA-256：6805b40de4d7214abe371d7dc0138b99bb5ab18e32165ed1b43dc92ed017f5e4
```

这里的判断方法很重要：基础设施错误通常出现在容器、网络或 Runner 初始化阶段；工程错误则会在 West Build、CMake、Kconfig 或 Devicetree 日志中给出具体文件和行号。不要看到红叉就立即乱改代码。

---

## 第 5 章：第一次刷入 GALPANEL 固件

### 5.1 先确认“固件、板子、目标”三者一致

第一次刷写前不要只看扩展名。必须同时确认：

```text
源提交：3bb605e515a98756e3b3c968b7e4dd3dfb4592be
构建目标：nice_nano//zmk + galpanel
Bootloader Board-ID：nRF52840-nicenano
待刷文件名：galpanel.uf2
```

本次下载的 GitHub 产物 ZIP：

```text
artifacts/firmware-run-1.zip
SHA-256: ee6b3bb7e22da461ac8674a448fc7acc3cd77c35aee1731773e1ed0be466aead
```

解压后的固件：

```text
artifacts/firmware-run-1/galpanel.uf2
大小：412160 bytes
SHA-256: 6805b40de4d7214abe371d7dc0138b99bb5ab18e32165ed1b43dc92ed017f5e4
```

UF2 文件头和块尾魔数也已验证：

```text
0x0A324655
0x9E5D5157
0x0AB16F30
```

这些检查相当于 STM32 烧录前确认 MCU 型号、工程配置、HEX 文件来源和校验值。

### 5.2 进入 UF2 Bootloader

当前空白板插入 USB 后会直接显示 `NICENANO`。以后正常固件已经运行时，按商品资料的方法操作：

```text
0.5 秒内让 RST 与 GND 短接两次
→ Windows 出现 NICENANO U 盘
```

载板上的 RST 键就是用来完成这个动作的。不要长时间短接，也不要短接 3.3 V、B+ 或其他 GPIO。

### 5.3 刷写前在 PowerShell 核验

假设启动盘仍为 E 盘：

```powershell
Get-Volume -DriveLetter E
Get-Content 'E:\INFO_UF2.TXT'
Get-FileHash -Algorithm SHA256 '.\artifacts\firmware-run-1\galpanel.uf2'
```

必须看到：

```text
FileSystemLabel : NICENANO
DriveType       : Removable
Board-ID        : nRF52840-nicenano
```

盘符可能改变，所以日常操作时应以卷标和 `INFO_UF2.TXT` 为准，不能永远假设是 E 盘。

### 5.4 备份与复制

第一次刷写前，本项目先备份了原有 Flash 的 UF2 表示：

```powershell
Copy-Item 'E:\CURRENT.UF2' '.\artifacts\factory-current-before-galpanel.uf2'
Copy-Item '.\artifacts\firmware-run-1\galpanel.uf2' 'E:\galpanel.uf2'
```

备份文件 SHA-256：

```text
9d2ac4f29706e9bee83a9ea772e2eb658f4025eb5e2d3823e1242cbaceb44fdb
```

复制 `galpanel.uf2` 后，不需要在 U 盘里双击或运行它。Bootloader 会直接把数据写入 Flash，然后自动重启；因此 U 盘突然消失是预期成功现象，不是“U 盘掉线”。

### 5.5 本次真实刷写结果

2026-07-26 实测：

```text
[通过] CURRENT.UF2 已备份
[通过] galpanel.uf2 已复制到 NICENANO 根目录
[通过] 复制后 NICENANO 自动消失
[通过] Windows 枚举 VID_1D50 / PID_615E
[通过] USB 总设备上报名称 GALPANEL
[通过] 枚举 Keyboard HID
[通过] 枚举 Consumer Control HID
[通过] 枚举 Mouse HID
```

这说明 ZMK 应用已经启动，USB 描述符有效，键盘、媒体键和鼠标滚轮三个报告通道均已建立。

### 5.6 接下来由你完成的安全实操

先打开记事本，再逐项测试，测试一个记录一个：

1. 按中机械键，应输入 `a`；
2. 按右机械键，应产生 Enter 换行；
3. 按 EC11，应产生 Esc；
4. 转动 EC11，页面或可滚动文本应上下滚动；
5. 左机械键单独是 Ctrl，不会显示字符，可配合 Windows 屏幕键盘观察；
6. 最后快速双击 RST，确认 `NICENANO` 再次出现；
7. 再按一次 RST 或重新刷入同一 UF2，返回 GALPANEL 应用。

第一次测试不要先按 QSAVE、QLOAD 或桌面键，因为它们会触发 F11、F12、Win+D；先确认基础键和复位恢复链路。

### 5.7 左机械键“没有反应”时如何判断

左机械键的基础层功能是 `LCTRL`。Ctrl 属于修饰键，单独按住不会在记事本中产生字符，这和 STM32 键盘 HID 只发送 modifier bit、没有普通 keycode 是同一件事。

按以下顺序判断：

1. 打开 Windows 屏幕键盘，按住左机械键，观察 Ctrl 是否高亮；
2. 或按一次右下 Fn 侧键，再按左机械键；Fn 层中同一个 D2 会发送可见字符 `S`；
3. 如果能输入 `s`，说明 D2/P0.17、焊点、按键和扫描程序均正常，只是 Ctrl 单独没有文字；
4. 如果 Ctrl 不高亮、Fn 层也没有 `s`，再生成把 D2 临时映射为普通字母的诊断固件；
5. 诊断固件仍无输出时，重点检查 D2/P0.17 到按键、按键到 GND 的通断和焊点。

2026-07-26 已在 Bootloader 模式下重新刷入同一份已校验 UF2。复制后 `NICENANO` 正常消失，`GALPANEL` 三类 HID 接口重新枚举成功，因此没有发现固件文件损坏或刷写失败。

首次测试记录表：

|测试项|预期|实测|
|---|---|---|
|UF2 复制|复制完成后 U 盘自动消失|通过|
|设备名称|Windows 识别 GALPANEL/HID|通过|
|复合 HID|键盘、媒体控制、鼠标接口均存在|通过|
|左机械键|Ctrl 按下与释放|通过：按住后配合电脑 C 成功复制文字|
|中机械键|输出 A|待测|
|右机械键|输出 Enter|待测|
|EC11 按压|输出 Esc|待测|
|EC11 旋转|产生上下滚轮|待测|
|双击 RST|重新出现 NICENANO|待测|

测试时可打开记事本或键盘测试网页，但 Ctrl 单独按下不会显示字符，需要配合屏幕键盘或键盘测试工具观察按下/释放状态。

### 5.8 为什么现在先做蓝牙

当前还没有焊载板，D3、D4、D19、EC11 和侧键不方便系统测试，但 BLE 广播、配对、绑定和重连只依赖开发板本身。此时先做蓝牙，可以继续推进软件，同时不把“未焊 GPIO”误当成软件阻塞。

当前固件已经具备：

- ZMK 默认 BLE HID；
- 多个 BLE Profile；
- SYS 单击 `BT_NXT`，切换到下一个 Profile；
- SYS 双击 `OUT_TOG`，切换 USB/BLE 输出；
- 键盘、媒体控制和鼠标复合 HID 描述。

注意：当前固件只在 `galpanel.overlay` 中声明了五颗 `gpio-leds`，没有状态事件监听或 LED API 调用。因此 `INFO/LINK/FN/WARN/AUX` 默认都不会亮。这个状态和 SuperMini 板载红/蓝指示灯完全独立。

按最新焊接原理图，GALPANEL LED 颜色固定为：

|网络|颜色|ProMicro 引脚|
|---|---|---|
|INFO|蓝|D9/P1.06|
|LINK|绿|D6/P1.00|
|FN|白|D15/P1.13|
|WARN|红|D1/P0.06|
|AUX|蓝（默认 DNP）|D0/P0.08|

五颗 LED 的阴极汇合到 `LED_GND`，再经过 `R_LED_EN`（0 Ω）接系统 GND。若将来做“全灯点亮”诊断而仍全部不亮，第一优先检查 `R_LED_EN` 是否已焊接、公共焊盘是否连通，以及 LED 极性；不要先怀疑五个 GPIO 同时损坏。

第一轮蓝牙测试不需要重新编译固件。

#### 第一步：Windows 首次配对

1. 确认开发板正在运行 GALPANEL，而不是显示 `NICENANO` U 盘；
2. 打开“设置 → 蓝牙和设备 → 添加设备 → 蓝牙”；
3. 等待列表中出现 `GALPANEL`；
4. 点击并完成配对；
5. 先不要删除配对，也不要反复切换 Profile。

可以把它和 STM32/BLE 工程这样对应：

|BLE 概念|如何理解|
|---|---|
|Advertising|外设周期发送“我叫 GALPANEL，可以连接”|
|Connection|Windows 已建立临时无线链路|
|Bonding|双方把加密密钥保存到 Flash，下次无需重新确认|
|Profile|ZMK 保存的不同主机绑定槽位，不是键位层|
|Output|当前 HID 报告发往 USB 还是 BLE|

#### 第二步：证明输入确实走 BLE

开发板通过电脑 USB 连接时，ZMK 可能优先把按键报告发送给 USB。仅看到“蓝牙已连接”还不能证明按键走的是无线。

可靠测试方法：

1. 完成配对后，从电脑拔下开发板；
2. 把开发板接到 USB 充电器、移动电源或只有供电没有数据的 USB 口；
3. 等待 Windows 显示 `GALPANEL` 已连接；
4. 将 D2 短接到 GND并保持；
5. 在电脑键盘上按 `C`；
6. 如果成功复制文字，Ctrl 只能来自 BLE，纯无线输入闭环通过。

#### 第三步：断电自动重连

1. 断开开发板电源；
2. 等待 3～5 秒；
3. 再次接入充电器或移动电源；
4. 等待 Windows 自动显示已连接；
5. 重复 D2→GND 加电脑 `C` 的复制测试。

不需要重新“添加设备”就能工作，说明 bonding 和自动重连正常。

#### 本次实操记录

2026-07-26，Windows 已成功搜索并配对 `GALPANEL`，D2→GND 对应的 Ctrl 能通过 BLE 正常工作：按住 D2 后配合电脑键盘 `C`，复制操作成功。

```text
[通过] Windows 搜索到 GALPANEL
[通过] 首次 BLE 配对
[通过] BLE Keyboard HID
[通过] D2/P0.17 通过 BLE 发送左 Ctrl
[待测] 断电上电自动重连
[待测] 多 Profile
[待测] SYS 双击切换 USB/BLE 输出
```

这证明当前固件不是“USB 版”和“蓝牙版”两份程序，而是同一个 ZMK 双模固件根据输出状态把 HID 报告送往 USB 或 BLE。

#### 第四步：以后测试 SYS

SYS 位于 D8/P1.04。裸开发板测试时不要用镊子跨越较远引脚，准备一根绝缘跳线，一端接任意 GND，另一端轻触 D8：

- 单击一次：切换下一个 BLE Profile；
- 300 ms 内双击：切换 USB/BLE 输出。

Profile 测试建议先让 Windows 使用 Profile 0，再切到 Profile 1 与手机配对。切回 Profile 0 后应重新连接 Windows，且不能破坏 Profile 1 的手机绑定。

当前没有开放清除配对功能，因此短按或双击 SYS 不会擦除 bond。后续只有在红色警告灯和长按确认都完成后，才增加这个危险操作。

#### 常见现象

|现象|优先检查|
|---|---|
|搜索不到 GALPANEL|确认不在 Bootloader；复位一次；删除过期的旧设备后重新搜索|
|显示已连接但按键仍走 USB|改用充电器/移动电源供电，或用 SYS 双击切换输出|
|配对后出现键盘但没有鼠标/媒体接口|删除旧配对并重新配对，让 Windows 刷新 HID 描述|
|断电后不能自动重连|确认仍在原 Profile，Windows 没有主动断开或删除设备|
|切换 Profile 后像是“失联”|新 Profile 尚未与任何主机绑定，属于正常等待配对状态|

---

## 第 6 章：知识迁移目标

你已经会 STM32 基础、CubeMX 配置和 HAL 库，这意味着你已经掌握了嵌入式开发最重要的几项基础：GPIO、时钟、中断、外设、烧录和调试。

这次并不是从零重新学嵌入式，而是把已有知识迁移到另一套组织方式：

```text
STM32 + CubeMX + HAL
        ↓ 知识迁移
nRF52840 + Zephyr + ZMK
```

底层电气概念没有变，变化最大的是“工程由谁生成、硬件在哪里描述、功能由谁提供”。

## 第 7 章：STM32 与 ZMK 第一张对照表

|你熟悉的 STM32 世界|GALPANEL 使用的世界|如何理解|
|---|---|---|
|STM32CubeMX|Devicetree + Kconfig|描述硬件连接并开启功能|
|`.ioc` 文件|`.overlay`、`.dtsi`、`.conf`|工程的硬件和功能配置源|
|HAL GPIO 初始化|Zephyr GPIO 驱动|不需要自己逐个写初始化函数|
|HAL USB Device/HID|ZMK USB HID|ZMK 已经实现键盘协议|
|自己移植 BLE 协议栈|ZMK + Zephyr Bluetooth|配对、重连和 Profile 已封装|
|Keil Build|West/CMake/Ninja Build|Zephyr 的标准构建链|
|Keil 工程文件|ZMK config 仓库|日常主要维护配置而不是 IDE 工程|
|J-Link Download|UF2 拖拽烧录|日常刷写更简单|
|J-Link 调试|J-Link + SWD|仍然保留，主要用于救砖和底层调试|
|`main()` 轮询|事件、驱动、线程和行为|大部分功能由框架响应事件|
|HAL 回调函数|ZMK/Zephyr 事件监听|后续 LED 状态模块会使用|

## 第 8 章：为什么这次不使用 Keil5

Keil5 当然能为 nRF52840 编写裸机或厂商 SDK 工程，但这样需要自己完成：

- USB 键盘 HID；
- BLE HID；
- 配对、绑定和重连；
- 多蓝牙设备 Profile；
- USB/BLE 输出选择；
- 键位层和宏；
- 鼠标滚轮和媒体键报告；
- 休眠、唤醒和功耗管理；
- 配置保存。

这些内容远比扫描几个 GPIO 复杂。ZMK 已经为键盘场景提供了成熟实现，所以我们的工作重点变为：

```text
描述硬件连接 + 定义按键行为 + 验证实物
```

这和使用 HAL 库而不是自己操作所有寄存器是同一个思想，只是抽象层更高。

## 第 9 章：Devicetree 是什么

在 CubeMX 中，你会用图形界面选择某个引脚是 GPIO_Input、UART_TX 或 TIM_CH。CubeMX 再生成初始化代码。

Zephyr 使用 Devicetree 文本描述硬件。例如 GALPANEL 的左机械键：

```dts
<&pro_micro 2 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>
```

可以拆成：

- `&pro_micro`：使用 ProMicro 引脚映射；
- `2`：D2；
- `GPIO_ACTIVE_LOW`：按下时为低电平；
- `GPIO_PULL_UP`：启用内部上拉。

它对应的 HAL 思维大概是：

```c
GPIO_InitStruct.Pin = KEY_FAST_Pin;
GPIO_InitStruct.Mode = GPIO_MODE_INPUT;
GPIO_InitStruct.Pull = GPIO_PULLUP;
HAL_GPIO_Init(KEY_FAST_GPIO_Port, &GPIO_InitStruct);
```

区别是初始化代码由 Zephyr 根据 Devicetree 自动生成和管理。

## 第 10 章：Kconfig `.conf` 是什么

`.conf` 用来决定哪些软件模块参与编译。例如：

```ini
CONFIG_EC11=y
CONFIG_ZMK_POINTING=y
```

意思分别是：

- 编译 EC11 编码器支持；
- 编译鼠标/滚轮 HID 支持。

这类似于 CubeMX 中勾选某个中间件，或者在工程中打开一个预编译宏。

## 第 11 章：Keymap 是什么

`.keymap` 把物理输入绑定到行为：

```dts
&kp ENTER
```

表示按下时发送 Enter，松开时释放 Enter。ZMK 会同时处理 USB 和 BLE 报告，不需要分别写两套代码。

Fn 层可以理解成一个“当前生效的映射表”：

```text
基础层：Ctrl / A / Enter
Fn 层： S   / R / L
```

它不是修改 GPIO，而是让相同物理位置在不同层调用不同的行为。

## 第 12 章：EC11 为什么分成两部分

EC11 实际包含：

- 一个普通按压开关；
- A/B 两相信号组成的旋转编码器。

所以软件中：

- 按压脚 D19 被放在普通按键扫描列表中；
- D21/D20 被描述为 `alps,ec11` sensor；
- keymap 中的 `sensor-bindings` 决定旋转行为。

当前按 20 脉冲编码器设置：

```dts
steps = <80>;
triggers-per-rotation = <20>;
```

这是因为 A/B 正交编码每个完整脉冲可以观察到四个边沿。若实物每圈触发数量不正确，优先核对商家参数和旋转实测。

## 第 13 章：UF2 与 J-Link 的关系

### UF2

日常开发流程：

```text
双击 RST
→ 开发板变成 USB U 盘
→ 拖入 galpanel.uf2
→ 自动重启
```

优点是简单，不需要下载器。

### J-Link/SWD

当 Bootloader 损坏、刷错底层固件或无法进入 UF2 时：

```text
J-Link
→ SWDIO / SWCLK / 3V3 / GND
→ 擦除或重新刷 Bootloader
```

这与你以前用 J-Link 给 STM32 烧录和救砖很接近。区别只是目标芯片换成 nRF52840，工具可使用 nRF Connect Programmer、`nrfjprog` 或 J-Link Commander。

## 第 14 章：Git 和 GitHub Actions 是什么关系

Git 负责在本地记录版本：

```text
工作区修改
→ git add
→ git commit
```

GitHub 是远端仓库。推送后：

```text
git push
→ GitHub Actions 读取 build.yaml
→ 下载 ZMK/Zephyr
→ 编译
→ 生成 galpanel.uf2
```

因此初期不安装完整编译器也可以工作。GitHub Actions 相当于一台每次都从干净环境开始构建的远程编译电脑。

## 第 15 章：第一次固件测试应做什么

不要一上来测试所有宏。第一次刷板只关注四件事：

1. 能否双击 RST 进入 UF2；
2. 固件能否启动；
3. USB 是否识别为键盘；
4. D2、D3、D4、D19 是否分别输出 Ctrl、A、Enter、Esc。

如果这四件事正常，说明：

- Board target 基本正确；
- Bootloader 和 ZMK 固件兼容；
- USB 正常；
- ProMicro 引脚映射方向正确；
- Direct GPIO 扫描正常。

然后再加 BLE 和旋钮，定位问题会容易很多。

## 第 16 章：关于 LED 电阻和 PWM

PWM 可以调暗 LED，但电阻仍然决定峰值电流和允许的最高亮度。

可以这样理解：

```text
限流电阻：规定每次点亮时最多流多少电流
PWM：规定一段时间内亮多久、灭多久
```

所以使用 2.2 kΩ 后再以 10% PWM 点亮，与使用 4.7 kΩ 常亮不是完全相同，但二者都能降低平均亮度和功耗。对于夜间设备，4.7 kΩ 会提供更保守的峰值电流；如果 LED 经过顶板透光孔太暗，再降低阻值。最终应以实物颜色和结构测试决定。

## 第 17 章：当前学习结论

- 你原有的 GPIO、上拉、SWD、消抖和外设知识仍然有效；
- Devicetree 替代了大量 CubeMX 引脚初始化工作；
- Kconfig 决定编译哪些驱动和功能；
- Keymap 描述用户操作，不需要手写 USB/BLE HID；
- UF2 是日常烧录，J-Link 是底层保障；
- GitHub Actions 可以先代替本地复杂工具链；
- 学习重点应从“寄存器和 HAL 调用”转向“设备树、行为、事件和验证方法”。

## 第 18 章：第一次独立硬件测试固件——LED_TEST

正式固件和测试固件要分开。正式固件回答“产品功能是否正确”，LED_TEST 只回答“这五路 LED 电气通路是否成立”。变量越少，出现问题时越容易定位。

本次 LED_TEST 使用 Zephyr 的 GPIO hog。可以把它理解为：系统初始化 GPIO 控制器时，立即把 D9、D6、D15、D1、D0 配成输出高电平，并一直保持。它不需要按键回调、BLE 事件或 PWM，作用类似在 STM32 的 GPIO 初始化完成后连续执行五次 `HAL_GPIO_WritePin(..., GPIO_PIN_SET)`。

本次云端构建经历了一个典型 Kconfig 错误：

```text
BLE device name is too long. Max length: 16
```

原名 `GALPANEL LED TEST` 超过 ZMK 的 16 字节限制，改为 `GP LED TEST` 后，Run #5 的正式固件和 LED_TEST 都构建成功。这也说明阅读编译日志时应先找最后一个明确的 `error:`，不要看到构建失败就先怀疑 GPIO 或焊接。

烧录前做了三层核验：

1. GitHub artifact ZIP 的 SHA-256 与服务器摘要一致；
2. `galpanel-led-test.uf2` 长度是 512 字节的整数倍，UF2 双魔数正确；
3. 目标盘为 `NICENANO`，`Board-ID` 是 `nRF52840-nicenano`。

复制完成后 `E:` 自动消失，表示 Bootloader 已接收文件并跳转到应用。此时需要观察的是 GALPANEL 上的 INFO 蓝、LINK 绿、FN 白、WARN 红、AUX 蓝，而不是 SuperMini 模块自己的红蓝灯。

实物判断规则：

- 五颗全亮：GPIO、公共 LED_GND、R_LED_EN 和主要焊接通路通过；
- 五颗全灭：优先检查 R_LED_EN、LED_GND 和整体 LED 极性；
- 只有部分不亮：按 D9、D6、D15、D1、D0 逐路检查 LED 极性、限流电阻和焊点；
- 出现发热或 USB 反复断连：立即拔掉 USB，不继续带电测试。

### 18.1 实物结果

2026-08-31，用户确认五颗 GALPANEL LED 全部常亮。这个结果不是简单的“灯会亮”，而是一次并行链路验证：五路 GPIO 都能输出、五组限流与 LED 支路均导通、公共 `LED_GND/R_LED_EN` 回路也导通。

不过，全亮测试尚不能完全证明每一路都能独立关断。下一步逐灯循环测试相当于 STM32 中依次 `SET/RESET` 每个输出，可以进一步排除相邻网络短接，并核对颜色与 GPIO 映射。

## 第 19 章：后三个诊断固件怎么分工

这三个固件不是三份“产品程序”，而是三把不同的万用表：

| 固件 | 主要回答的问题 | 输出方式 |
|---|---|---|
| `galpanel_led_ind_test` | 五颗 LED 能否独立控制 | HID Num/Caps/Scroll/Compose/Kana 指示灯 |
| `galpanel_io_test` | 九路按键焊接和扫描是否正确 | 依次输出数字 `1～9` |
| `galpanel_ec11_test` | EC11 A/B、方向、步数和按压是否正确 | 旋转输出 `Up/Down`，按压输出 `9` |

### 19.1 为什么 LED 独立测试使用 HID 指示位

全亮测试只能证明“每路都能点亮”。独立测试需要一个来自主机的开关信号，HID 指示灯正好提供了这条路径：主机改变 Num Lock 等状态，ZMK 收到 HID 输出报告，再更新对应 LED。前三路在 Windows 上容易直接观察；Compose/Kana 是标准 HID 指示位，但常规桌面环境通常不会主动改变它们，必要时使用 HID 测试工具发送输出报告。

### 19.2 为什么 IO_TEST 输出数字

Ctrl、F11、Win+D 这类键的结果不容易在记事本里直接看出。把物理输入映射为 `1～9` 后，按键测试变成最简单的“输入—字符”对应关系；保持按下、松开和再次按下也能直观看到消抖是否正确。

### 19.3 为什么 EC11_TEST 保留九路输入

旋钮测试不应重新引入一套不同的按键扫描配置。保留九路数字输出，可以在一次烧录中同时确认普通输入、EC11 按压和 A/B 相位。若旋转方向相反，只需交换软件绑定的 `UP/DOWN`，不需要改 PCB。

### 19.4 云端编译结果

Run #6 已将正式固件和三套新测试固件全部编译成功。下载包中包含：

```text
galpanel-led-ind-test.uf2
galpanel-io-test.uf2
galpanel-ec11-test.uf2
galpanel-led-test.uf2
galpanel.uf2
```

这一步相当于把“设计稿”变成了可执行的 nRF52840 固件。回家后不需要重新编译，只要让开发板进入 `NICENANO` 盘符，再把对应 UF2 拖进去即可。
