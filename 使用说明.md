# GALPANEL V2.1 使用说明（最终功能审核稿）

## 1. 产品定位

GALPANEL 是一款基于 SuperMini/ProMicro nRF52840 的 GALGAME 专用控制器，支持：

- USB 有线 HID；
- Bluetooth Low Energy（BLE）无线 HID；
- 5 个 BLE Profile；
- 3 个机械键、4 个侧键、SYS 键、FN 键；
- EC11 旋钮旋转和按压；
- 键盘、鼠标滚轮和媒体键复合 HID；
- 5 颗状态指示灯。

本文是最终功能设计的审核稿。已经通过实物验证的功能会标为“已验证”；只写入设计、尚未完成最终固件验证的功能会标为“计划实现”。

---

## 2. 按键和旋钮布局

### 2.1 基础层（GALGAME）

| 控件 | 原理图引脚 | 基础层功能 | 说明 |
|---|---:|---|---|
| 左机械键 | D2 | `LCTRL` | 按住时发送 Ctrl |
| 中机械键 | D3 | `A` | 普通字母 A |
| 右机械键 | D4 | `Enter` | 回车 |
| 右上侧键 | D5 | 双击 `F11` | QSAVE；单击无输出，防止误触 |
| 右下侧键 | D7 | 双击 `Win+D` | 显示/隐藏桌面；单击无输出 |
| SYS | D8 | 单击切换 BLE Profile；双击切换 USB/BLE 输出 | 详见第 4 节 |
| 左下侧键（FN） | D14 | 切换 FN 层 | 再按一次返回基础层 |
| 左上侧键 | D18 | 双击 `F12` | QLOAD；单击无输出，防止误触 |
| EC11 按压 | D19 | `Esc` | 旋钮按下 |

### 2.2 FN 层

FN 层由 D14 切换。进入 FN 层后，白色 FN 灯应常亮；退出后熄灭。

| 控件 | FN 层功能 |
|---|---|
| 左机械键 | `S` |
| 中机械键 | `R` |
| 右机械键 | `L` |
| 右上侧键 | 双击 `F11` |
| 右下侧键 | 双击 `Win+D` |
| SYS | 与基础层相同 |
| 左下侧键（FN） | 返回基础层 |
| 左上侧键 | 双击 `F12` |
| EC11 按压 | 静音 |

### 2.3 EC11 旋钮

每一个明显机械刻度对应一次事件，不按整圈计数。

| 状态 | 顺时针 | 逆时针 |
|---|---|---|
| 基础层 | 鼠标滚轮下滚 | 鼠标滚轮上滚 |
| FN 层 | 音量增加 | 音量减少 |

EC11 方向已经按当前实物测试结果定义。如果后续更换旋钮导致方向相反，只修改软件绑定，不修改 PCB。

---

## 3. 五颗状态灯的最终定义

LED 只表示 GALPANEL 状态，不使用 SuperMini 模块自带的红灯、蓝灯作为产品状态判断。

| LED | 颜色 | 最终含义 | 计划中的表现 |
|---|---|---|---|
| INFO | 蓝 | 当前 BLE Profile / 操作提示 | 上电或切换 Profile 后闪烁 1～5 次，对应 Profile 1～5 |
| LINK | 绿 | BLE 链路状态 | 正在广播或等待连接慢闪；连接成功亮 30 秒后熄灭；断开后重新慢闪 |
| FN | 白 | 当前是否处于 FN 层 | FN 层常亮，基础层熄灭 |
| WARN | 红 | 错误或危险操作反馈 | 配对清除倒计时、清除完成或异常时闪烁 |
| AUX | 蓝 | 当前实际 HID 输出端点 | USB 输出亮；BLE 输出或无可用端点灭 |

### 3.1 AUX 的判定规则

AUX 不检测“有没有 USB 供电”，而检测 ZMK 当前选择的实际 HID endpoint：

```text
AUX 亮：按键和旋钮报告当前发往 USB
AUX 灭：报告当前发往 BLE，或暂时没有可用连接
```

因此：

- 插普通充电器但没有 USB 数据连接时，AUX 不应因为供电而误亮；
- 插电脑并成功枚举 USB HID 时，AUX 可能自动点亮；
- USB 和 BLE 同时可用时，SYS 双击切换输出，AUX 应随实际端点改变。

### 3.2 LED 优先级

LED 状态不能互相覆盖，优先级固定为：

1. WARN 的危险操作/错误提示；
2. INFO 的 Profile 或操作提示；
3. LINK、FN、AUX 的持续状态。

提示动画结束后，LINK、FN、AUX 必须恢复到实时状态。INFO 和 WARN 的闪烁不能改变 HID 输出，也不能阻塞按键扫描。

---

## 4. USB、蓝牙和输出切换

### 4.1 USB 使用

1. 使用 USB-C 数据线连接电脑；
2. 等待系统枚举 GALPANEL 键盘和鼠标 HID；
3. AUX 亮表示当前实际输出走 USB；
4. 可以直接使用基础层、FN 层和 EC11 功能。

USB 连接同时负责供电和 UF2 烧录。普通充电线或充电器可能只有电源，没有 USB HID 数据通道。

### 4.2 BLE 首次配对

1. 断开 USB 数据线，使用电池或移动电源供电；
2. 等待 LINK 绿灯慢闪，表示正在广播/等待连接；
3. 在电脑或手机蓝牙设置中搜索对应的 `GALPANEL 1`～`GALPANEL 5`；
4. 完成配对；
5. LINK 绿灯亮起表示 BLE 已连接；连接成功约 30 秒后自动熄灭以节省电量；
6. AUX 熄灭表示当前按键输出走 BLE。

首次启用新的 HID 描述（例如新增鼠标滚轮能力）后，主机可能需要删除旧的 GALPANEL 配对记录，再重新配对。

### 4.3 BLE Profile

GALPANEL 提供 5 个独立 BLE Profile。每个 Profile 可以保存一台主机的配对关系。当前 Profile 的广播名称分别为 `GALPANEL 1`、`GALPANEL 2`、`GALPANEL 3`、`GALPANEL 4`、`GALPANEL 5`。

- SYS 单击：切换到下一个 Profile；
- INFO 蓝灯：切换后闪烁对应次数，帮助确认当前 Profile；
- 蓝牙设置中按名称区分 Profile，例如 `GALPANEL 1` 对应 Profile 1；
- 切换 Profile 不应清除其他 Profile 的配对记录；
- 如果当前 Profile 没有配对设备，LINK 会继续慢闪，直到该 Profile 配对成功。

### 4.4 USB/BLE 输出切换

- SYS 双击：切换首选输出端点；
- AUX 亮：当前实际输出为 USB；
- AUX 灭：当前实际输出为 BLE 或没有可用端点；
- 首选端点不可用时，ZMK 可能回退到另一个已连接端点，因此最终以 AUX 和实际按键结果为准。

这里的 USB/BLE 输出切换不会删除配对信息，也不会切换 BLE Profile。

---

## 5. 电源和充电注意事项

### 5.1 当前支持的供电方式

- USB-C 供电：适合开发、烧录和 USB 使用；
- 单节锂电池：适合 BLE 无线使用，标称电压 3.7 V，满电约 4.2 V；
- 移动电源：可作为无线测试电源，但应确认其不会在低电流时自动断电。

`BAT+`、模块 `B+` 和 `RAW` 属于同一电池正极路径；`B-` 应回到系统 GND。不要额外把电池正极和 RAW 外部短接，也不要把电池负极悬空。

### 5.2 安全规则

- 不建议可调电源和 USB-C 长时间同时连接；
- 接电池前先断开 USB，检查极性和焊点；
- 发现异常发热、反复断连、明显压降或 LED 异常变亮，立即断电；
- ADC 电量检测当前不属于正式功能，WARN 暂不代表精确电量；
- 天线距离、外壳和旋钮对 BLE 的影响，需要在整机装配后单独测试。

---

## 6. 固件更新和恢复

### 6.1 日常刷入正式固件

1. 从 GitHub Actions 下载正式构建产物中的 `galpanel.uf2`；
2. 快速按两次 RST；
3. 等待出现卷标为 `NICENANO` 的 UF2 U 盘；
4. 将 `galpanel.uf2` 拖入该盘；
5. 盘符自动消失表示 Bootloader 已接受文件并重启；
6. 重新测试 USB 或 BLE。

不要把 `galpanel-led-test.uf2`、`galpanel-io-test.uf2` 或 `galpanel-ec11-test.uf2` 当作日常固件使用。

### 6.2 BLE 无法连接时的恢复流程

清除配对属于破坏性操作，只在普通 Profile 切换无法恢复时使用：

1. 刷入 `galpanel-ble-recovery.uf2`；
2. 使用恢复固件的 `BT_CLR_ALL` 清除设备端全部 bond；
3. 在 Windows/手机中删除旧的 `GALPANEL` 设备；
4. 刷回正式 `galpanel.uf2`；
5. 搜索正式设备名 `GALPANEL` 并重新配对；
6. 断电后重新上电，验证自动重连。

恢复固件只用于维修，不是产品运行固件。

---

## 7. 故障判断

| 现象 | 优先检查 |
|---|---|
| USB 能用、BLE 不能用 | 当前 Profile、主机旧 bond、是否在正式固件下重新配对 |
| LINK 慢闪但电脑搜不到 | 供电、天线遮挡、主机蓝牙开关、当前 Profile 是否为空 |
| LINK 常亮但按键无效 | AUX 当前状态、主机是否连接了另一个 Profile、是否切到了错误输出端点 |
| AUX 亮 | 当前 HID 输出走 USB，不代表 BLE 未连接 |
| AUX 灭 | 当前 HID 输出走 BLE，或没有可用 HID 端点 |
| FN 灯与按键层不一致 | 重新按 D14，记录是否为旧固件；必要时重新刷正式固件 |
| 单击触发 F11/F12/Win+D | 检查双击节奏，确认没有刷入测试固件；目标窗口约 300 ms 内完成双击 |
| EC11 方向相反 | 记录方向后修改软件绑定，不改 PCB |
| 烧录后盘符不消失 | 确认卷标为 `NICENANO`、文件是对应板型 UF2，必要时重新双击 RST |

出现发热、焦味、USB 反复重连或电源明显压降时，先断电，再进行电气检查。

---

## 8. 最终功能验收顺序

建议按以下顺序验收，避免多个变量同时变化：

1. USB 正式固件：D2/D3/D4、EC11 按压和滚轮；
2. 9 路输入：3 个机械键、4 个侧键、SYS、EC11 按压；
3. FN 层：白灯、S/R/L、音量和静音；
4. BLE 首次配对与断电自动重连；
5. 5 个 Profile 轮换；
6. SYS 双击切换 USB/BLE，观察 AUX；
7. LINK 连接灯、INFO Profile 提示、WARN 安全提示；
8. 电池供电、充电、电流和续航；
9. 装壳后 BLE 距离、旋钮和机械应力。

最终发布前，应以 GitHub Actions 生成的正式 `galpanel.uf2` 和对应 Git commit 为准，不使用聊天记录中旧的 UF2 文件。

---

## 9. 本审核稿需要确认的设计参数

以下参数已经给出推荐值，但在最终实现前需要确认：

| 项目 | 推荐值 |
|---|---|
| INFO Profile 提示 | Profile 1～5 分别闪烁 1～5 次 |
| LINK 等待连接 | 约 1 秒周期慢闪；连接成功后亮 30 秒 |
| SYS 长按清除配对 | 测试固件约 6 秒，仅清除当前 Profile；正式固件待安全测试通过后启用 |
| WARN 清除提示 | 长按期间红灯倒计时，完成后快速闪烁确认 |
| FN 自动退出 | 第一版保持手动切换；如需要再增加无操作超时 |
| AUX 无连接状态 | 熄灭 |
| ADC 电量显示 | 暂不实现 |

在这些参数确认后，再进入 LINK、INFO、FN、WARN 状态模块的正式编码和实物验收。
