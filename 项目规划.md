# GALPANEL 软件项目规划

## 1. 规划目标

本规划用于把已经基本定版的 GALPANEL V2.1 硬件逐步变成一套可编译、可烧录、可验证、可维护的 ZMK 固件。

开发原则是：

- 先让最小功能稳定工作，再逐项增加复杂行为；
- USB 测试先于 BLE，普通按键先于宏和灯效；
- 每个阶段都能形成可刷写的固件，不长期停留在“全部写完但无法定位问题”的状态；
- 危险操作必须有确认和反馈，尤其是清除蓝牙配对；
- 软件配置、硬件网络名和文档尽量使用同一套名称；
- 每次工作结束必须更新日志并提交 Git。

## 2. 技术栈

|层级|采用方案|作用|
|---|---|---|
|芯片|Nordic nRF52840|BLE、USB、GPIO、低功耗、SWD|
|开发模块|SuperMini/ProMicro nRF52840|提供 USB-C、Bootloader、充电和 ProMicro 引脚|
|RTOS/硬件抽象|Zephyr|设备树、驱动、线程、蓝牙、USB 和电源管理|
|键盘固件|ZMK|键位、层、BLE Profile、USB/BLE 输出和 HID|
|硬件描述|Devicetree|描述按键 GPIO、EC11 和 LED 引脚|
|功能配置|Kconfig `.conf`|开启 EC11、鼠标和休眠等功能|
|键位描述|`.keymap`|定义每个输入在不同层上的行为|
|构建|GitHub Actions|自动安装工具链并生成 UF2|
|日常烧录|UF2 Bootloader|双击 RST 后拖入 UF2|
|救砖|J-Link + SWD|Bootloader 损坏或底层异常时恢复|

## 3. 软件目录约定

```text
GALPANEL/
├─ .github/workflows/       GitHub 自动编译
├─ config/
│  ├─ west.yml              ZMK/Zephyr 依赖清单
│  └─ boards/shields/
│     └─ galpanel/          GALPANEL 硬件和键位配置
├─ scripts/                 Windows 环境和结构检查脚本
├─ PCB/                     原理图与 PCB 导出资料
├─ SuperMini.../            官方芯片、Bootloader 和工具资料
├─ build.yaml               编译目标矩阵
├─ README.md                项目入口
├─ 项目认知.md              当前整体认知
├─ 项目规划.md              里程碑和验收标准
├─ galpanel.log.md          每轮工作的变更日志
└─ 学习日志.md              面向 STM32 背景的学习记录
```

## 4. 里程碑

### M0：仓库与工程骨架

状态：已建立，等待首次云端编译验证。

工作内容：

- 初始化 Git 仓库；
- 建立 ZMK shield 目录；
- 建立 GitHub Actions；
- 写入 V2.1 GPIO；
- 建立两层基础 keymap；
- 建立日志、学习文档和检查脚本。

验收标准：

- `scripts/validate-project.ps1` 通过；
- Git 工作树干净；
- 推送 GitHub 后能够生成 `galpanel.uf2`。

### M1：最小 USB 输入固件

目标：证明板型、Bootloader、GPIO 映射和 USB HID 路线正确。

仅启用并测试：

- 左机械键 `Ctrl`；
- 中机械键 `A`；
- 右机械键 `Enter`；
- EC11 按压 `Esc`；
- USB 键盘输出。

暂时不测试：BLE、旋转、双击、Fn、LED。

验收标准：

- UF2 可刷入且能重新进入 Bootloader；
- Windows 设备管理器能看到 HID 键盘；
- 四个输入没有串键、长按丢失和随机触发；
- 实际 GPIO 与原理图完全一致。

### M2：全部物理输入

目标：验证 V2.1 所有输入器件。

增加：

- 四颗侧键；
- SYS；
- EC11 A/B 旋转；
- 软件消抖和 RC 实物效果验证。

验收标准：

- 每颗输入都有独立测试键值；
- EC11 顺逆方向正确；
- 快速旋转 20 圈不出现明显漏步或反跳；
- 若方向反了，只在 keymap 中交换行为，不改 PCB。

### M3：BLE 与输出切换

目标：完成真正的双模控制器。

增加：

- BLE 广播、配对和自动重连；
- 多 BLE Profile；
- SYS 单击切换 Profile；
- SYS 双击切换 USB/BLE 输出；
- USB 和 BLE 两种路径的键盘输入测试。

验收标准：

- Windows 和手机均可配对；
- 断电重启后可以自动重连；
- USB/BLE 输出切换结果可预测；
- 不会因为切换 Profile 清除已有绑定。

### M4：GALGAME 键位和复合 HID

目标：实现完整基础层和 Fn 层。

增加：

- 基础层：Ctrl、A、Enter、Esc；
- Fn 层：S、R、L、静音；
- EC11 基础层鼠标滚轮；
- EC11 Fn 层音量；
- Fn 锁定与退出；
- QSAVE/QLOAD/Win+D 双击保护。

验收标准：

- Windows 中同时存在键盘、鼠标滚轮和媒体键能力；
- 修改 HID 描述后重新配对一次即可正常使用；
- 双击窗口在 280～320 ms 内可稳定触发；
- 单击不会触发 QLOAD、QSAVE 或 Win+D；
- Fn 层不改变四颗侧键的固定功能。

### M5：状态灯和安全行为

目标：让设备状态可见，并安全实现破坏性操作。

计划实现：

- 蓝灯：BLE/Profile；
- 绿灯：USB 和操作确认；
- 白灯：真实 Fn 层状态；
- 红灯：错误和危险操作确认；
- AUX 默认关闭；
- 上电 LED 自检；
- SYS 长按状态查询；
- SYS 长按约 6 秒清除当前 Profile；
- Fn 长时间无操作后自动退出。

这一阶段可能需要编写 ZMK/Zephyr 事件监听模块，而不仅是修改 keymap。

验收标准：

- 灯效反映真实系统状态，重启后不会与状态错位；
- 白灯可用 PWM 或占空控制保持低亮；
- 断开 `R_LED_EN` 后固件仍能正常工作；
- 清除配对前有清晰的红灯倒计时或确认反馈；
- 任何短按、双击和误触都不会清除配对。

### M6：功耗、机械与 RF 联调

目标：把“能用”收敛为“长期稳定使用”。

测试：

- USB、电池、开关组合；
- 活跃、待机和深睡眠电流；
- 拆除 `R_LED_EN` 前后功耗；
- 旋钮快速操作时的稳定性；
- 裸板、装壳、手遮挡、塑料/金属旋钮下的 BLE；
- USB 插拔和旋钮按压产生的板弯曲。

ADC 电量检测不在当前范围内。只有在基本功能和功耗稳定后，才重新评估是否增加。

### M7：首个可发布版本

目标版本：`v0.1.0`。

发布内容：

- 已验证的 ZMK 固件源码；
- 固定 ZMK 上游提交，不再直接跟随 `main`；
- `galpanel.uf2`；
- 刷写说明；
- 键位图；
- 硬件版本兼容说明；
- 已知问题和恢复方法。

## 5. 当前首版代码的有意取舍

首版 keymap 已经写入完整目标键位，便于尽早发现设备树和行为语法问题。但实际烧录测试必须仍按 M1 到 M4 分阶段进行。

SYS 当前只实现：

- 单击：下一 BLE Profile；
- 双击：切换 USB/BLE 输出。

没有立即实现长按清除配对，原因是：清除配对属于破坏性行为，在红灯反馈、长按计时和实物按键手感没有验证前，不应贸然开放。

LED 当前只完成 GPIO 的设备树定义，没有状态动画。原因是：LED 状态需要监听蓝牙、USB、层和系统事件，属于独立软件模块；把它与第一版 GPIO 调试绑在一起会显著增加定位难度。

## 6. Git 工作规范

### 分支

- `main`：始终保持可读、尽量可编译；
- 后续较大功能使用 `feature/功能名`；
- 紧急修复使用 `fix/问题名`。

### 提交类型

```text
feat: 新增可用功能
fix: 修复缺陷
docs: 文档和学习记录
test: 测试与验证脚本
refactor: 不改变行为的结构整理
chore: 构建、工具和仓库维护
```

示例：

```text
feat: add EC11 layer-aware bindings
fix: correct encoder direction on V2.1 hardware
docs: record first UF2 flashing workflow
```

### 每次工作完成流程

```text
1. 修改代码/文档
2. 运行能够运行的验证
3. 检查 git diff
4. 更新 galpanel.log.md
5. 若涉及新知识，更新 学习日志.md
6. 再次检查 git status
7. git add + git commit
```

日志必须回答：

- 做了什么；
- 为什么这么做；
- 对后续有什么好处；
- 如何验证；
- 还有什么未完成或风险。

## 7. 风险管理

|风险|应对|
|---|---|
|SuperMini 与 nice!nano 引脚/Bootloader 存在批次差异|先做 M1 最小 GPIO 测试；保留官方 Bootloader 和 SWD 资料|
|ZMK `main` 更新造成构建破坏|首次成功后固定到测试过的 commit|
|EC11 RC 导致漏步|M2 快速旋转实测，必要时调整触发数或电容|
|鼠标 HID 导致旧 BLE 描述缓存|删除 Windows 旧配对并重新配对|
|双击窗口不适合实际手感|将 300 ms 作为起点，用实物调整|
|Fn 锁定后忘记退出|白灯指示，并在 M5 增加自动退出|
|LED 影响续航|硬件限流 + PWM 调暗 + `R_LED_EN` 断开测试|
|误清蓝牙配对|长按、红灯反馈和延时确认同时完成后才开放|
|天线受 EC11/旋钮/外壳影响|M6 做装壳前后和手遮挡对比|

## 8. 近期下一步

1. 将当前 Git 仓库推送到用户自己的 GitHub；
2. 运行首次 GitHub Actions 构建；
3. 若构建失败，先修正 ZMK/Zephyr 语法，不急于刷板；
4. 构建成功后下载 UF2，按 M1 仅测试最小 USB 输入；
5. 在 `galpanel.log.md` 记录构建编号、UF2、板卡现象和每个 GPIO 的结果；
6. 首次成功后将 `config/west.yml` 的 ZMK revision 固定到已验证 commit。

