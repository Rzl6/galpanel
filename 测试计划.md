# GALPANEL V2.1 整板功能测试计划

## 1. 测试目标

本计划用于验证“电池以外已经全部焊接完成”的 GALPANEL V2.1 实物。目标不是一次性把所有功能都按一遍，而是把电气、输入、HID、蓝牙、LED、功耗和结构风险拆成可以独立判定的阶段。

当前已知基线：

- USB HID：已通过；
- BLE 配对和 D2→GND 的 Ctrl 输入：已通过；
- D3/D4 机械键和其余载板输入：等待整板焊接后验证；
- 电池、充电、电源开关和电池续航：暂不验收；
- GALPANEL LED：独立全亮测试固件已烧录，等待记录实物结果；
- SuperMini 板载红/蓝灯：只作为模块供电、Bootloader 或无线状态参考，不作为 GALPANEL LED 验收项。

## 2. 测试原则

1. 先断电检查，再上电测试；
2. 先测试公共电源/地，再测试单路 GPIO；
3. 先使用测试固件验证电气，再刷正式固件验证用户行为；
4. 每次只改变一个变量，失败时保留现象、固件版本和接线方式；
5. 不在未确认引脚身份时用镊子跨接，优先使用带绝缘层的单根跳线；
6. 所有破坏性操作（清除 BLE 配对、全片擦除、拆焊）必须单独确认并记录。

## 3. 固件分层

### 3.1 LED 电气测试固件

用途：验证 `INFO/LINK/FN/WARN/AUX` 五路 LED、串联电阻、LED 极性、`LED_GND` 和 `R_LED_EN` 公共回路。

当前第一版预期行为：上电后五路 GPIO 持续输出高电平，五颗 LED 应同时常亮。该固件只用于快速验证公共地、极性、限流电阻和 GPIO 通路，不承载正式键位，不用于日常使用。逐颗点亮与时序动画留到电气层通过后实现。

颜色和引脚以最新焊接原理图为准：

|顺序|网络|颜色|ProMicro 引脚|nRF52840 GPIO|预期|
|---:|---|---|---|---|---|
|1|INFO|蓝|D9|P1.06|单独点亮|
|2|LINK|绿|D6|P1.00|单独点亮|
|3|FN|白|D15|P1.13|单独点亮|
|4|WARN|红|D1|P0.06|单独点亮|
|5|AUX|蓝|D0|P0.08|单独点亮，默认 DNP 时跳过|

如果五颗同时不亮，优先检查 `R_LED_EN=0 Ω` 是否焊接、`LED_GND` 是否连到系统 GND；如果只有一颗不亮，再检查该颗 LED 极性、限流电阻和对应 GPIO。

### 3.2 IO 输入测试固件

用途：把九路普通输入改成可见数字，避免 `Ctrl`、`F11`、`Win+D` 等功能键不易观察。

建议映射：

|输入|引脚|测试输出|
|---|---|---|
|KEY_FAST|D2/P0.17|`1`|
|KEY_AUTO|D3/P0.20|`2`|
|KEY_NEXT|D4/P0.22|`3`|
|KEY_QSAVE|D5/P0.24|`4`|
|KEY_DESKTOP|D7/P0.11|`5`|
|KEY_SYS|D8/P1.04|`6`|
|KEY_FN|D14/P1.11|`7`|
|KEY_QLOAD|D18/P1.15|`8`|
|ENC_SW|D19/P0.02|`9`|

EC11 旋转建议输出 `Up/Down` 或 `[`/`]`，按压输出 `9`。每路按键都应满足：按下一次只出现一次、保持按下不连发、松开后可再次触发。

### 3.3 正式 GALPANEL 固件

用途：恢复正式功能并进行最终用户验收：

- 基础层：Ctrl、A、Enter、F11/F12/Win+D 双击、Esc；
- Fn 层：S、R、L、静音和音量；
- EC11：滚轮、音量；
- BLE：广播、配对、自动重连、Profile；
- SYS：Profile 切换和 USB/BLE 输出切换；
- LED：后续接入真实状态事件后再验收，不与 LED 电气测试混为一谈。

## 4. 分阶段执行顺序

### T0：断电电气预检

1. 不接电池，只连接 USB；
2. 目检是否有锡桥、反向 LED、虚焊、短路；
3. 万用表确认 3V3、VCC、GND 之间没有低阻短路；
4. 确认 `R_LED_EN` 已安装 0 Ω；
5. 确认 `LED_GND` 经 `R_LED_EN` 与系统 GND 导通；
6. 确认电池接口 `B+ / B-` 不接任何电池。

通过标准：无明显短路，USB 上电后模块不异常发热，Bootloader 或正式固件可以启动。

### T1：LED 电气测试

1. 刷入 LED 测试固件；
2. 观察五颗灯是否同时持续点亮；
3. 逐颗记录亮度、颜色和是否闪烁；
4. 当前五颗 LED 均已焊接，AUX 也应作为必测项记录；
5. 测试完成后刷回 IO 测试固件。

第一阶段通过标准：INFO、LINK、FN、WARN、AUX 五颗 LED 同时稳定点亮，且无明显压降、发热或 USB 反复断连。第一阶段通过后，再用逐灯版本确认每路能够独立控制。

### T2：九路按键输入

1. 刷入 IO 测试固件；
2. 打开记事本或键盘测试工具；
3. 按照 D2、D3、D4、D5、D7、D8、D14、D18、D19 顺序逐路测试；
4. 每路执行“短按、保持 2 秒、松开、再次短按”；
5. 记录是否有漏检、连发、串键或松开不释放。

通过标准：九路输入均输出指定数字，且每次动作与预期一一对应。

### T3：EC11 旋转与按压

1. 慢速顺时针旋转 1 圈；
2. 慢速逆时针旋转 1 圈；
3. 快速顺逆各旋转 20 圈；
4. 记录方向、每圈步数、漏步和反跳；
5. 按压 EC11 10 次，确认只输出一次按键。

通过标准：方向正确，20 圈快速测试无明显漏步或随机反向；若方向相反，只交换软件绑定，不修改 PCB。

### T4：USB 正式固件

1. 刷回正式 `galpanel.uf2`；
2. 验证 D2 Ctrl、D3 A、D4 Enter、EC11 Esc；
3. 验证滚轮、Fn、音量和双击宏；
4. 快速双击 RST，确认可以重新进入 `NICENANO`；
5. 再刷回同一 UF2，确认恢复 GALPANEL。

通过标准：基础输入、复位和复合 HID 均稳定，单击不会误触发双击宏。

### T5：BLE 基础闭环

1. Windows 删除旧的 GALPANEL 配对（仅在需要刷新 HID 描述时）；
2. 搜索并配对 GALPANEL；
3. 使用 USB 充电器或移动电源供电，排除 USB 数据路径；
4. 用 D2→GND + 电脑 Ctrl+C 验证纯 BLE 输入；
5. 断电 3～5 秒后重新上电，验证自动重连。

通过标准：无需重复添加设备即可重连，Ctrl、A、Enter 等基础键可以通过 BLE 工作。

### T6：Profile 与 USB/BLE 输出

1. 使用绝缘跳线短接 D8/SYS 一次，切换下一个 BLE Profile；
2. 在第二个 Profile 与手机配对；
3. 回到第一个 Profile，确认 Windows 绑定仍存在；
4. 在 USB 连接下对 SYS 做 300 ms 内双击，验证 `OUT_TOG`；
5. 记录切换后的实际输出端，不凭 LED 颜色猜测。

通过标准：Profile 之间互不覆盖，输出切换可预测，不会清除已有绑定。

### T7：USB 供电与整板安全

电池尚未焊接，本阶段只验收：

- USB 插拔是否稳定；
- LED 全亮/全灭时是否断连；
- 旋钮按压是否使 PCB 明显弯曲；
- 模块和 LED 电阻是否异常发热；
- 关闭外部 LED 公共回路后，主控是否仍能正常工作。

电池方向、充电电流、开关断开、待机电流和续航留到电池焊接后单独验收，禁止用裸线代替电池长期测试。

### T8：RF 与机械联调

1. 裸板 BLE 连接距离；
2. 装入顶板和底板后的连接距离；
3. 手遮挡天线、旋钮靠近天线、金属/塑料旋钮对比；
4. USB 插拔和旋钮按压时观察焊点、排针和 PCB 应力；
5. 记录断连、重连时间和异常方向。

## 5. 测试记录模板

每一项都记录以下字段：

```text
日期：
固件：LED_TEST / IO_TEST / GALPANEL
Git commit：
供电：USB / 电池（当前禁用）
主机：Windows / 手机
接线：按键直接短接 / 整板焊接
现象：
结论：通过 / 失败 / 待复测
下一步：
```

## 6. 固件切换与恢复

```text
进入 Bootloader
→ 备份 CURRENT.UF2（第一次测试前）
→ 刷 LED_TEST 或 IO_TEST
→ 记录结果
→ 重新进入 Bootloader
→ 刷回正式 galpanel.uf2
→ 验证 USB/BLE
```

任何测试固件都不能覆盖原始 `CURRENT.UF2` 备份。LED 测试固件和 IO 测试固件必须使用独立 artifact 名称，并在文件名中带上测试类型，避免把诊断固件误刷成正式固件。

## 7. 当前执行状态

|阶段|状态|
|---|---|
|T0 断电电气预检|待用户确认实物检查|
|T1 LED 电气测试固件|Run #5 构建通过并已烧录，等待记录五灯实物结果|
|T2 IO 输入测试固件|计划实现|
|T3 EC11 测试|待 T2 通过|
|T4 USB 正式固件|已完成基础 USB 闭环，待整板复测|
|T5 BLE 基础闭环|已通过首次配对和 Ctrl，待重连复测|
|T6 Profile/输出|待测|
|T7 USB 供电安全|待测|
|T8 RF/机械|待电池和外壳条件稳定后|
