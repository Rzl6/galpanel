# GALPANEL 项目认知

## 1. 项目定位

GALPANEL 是一款面向 Windows GALGAME/视觉小说场景的桌面专用控制器。它不是通用全尺寸宏键盘，而是围绕“推进、快进、自动播放、存读档、对话记录、音量和窗口管理”等高频操作设计的小型输入设备。

项目的核心目标是：

- 同时支持 USB 有线和 BLE 蓝牙；
- 支持键盘、鼠标滚轮和媒体键组成的复合 HID；
- 用少量高频机械键、侧键、EC11 编码器和 Fn 层覆盖常用操作；
- 使用 PCB、铜柱和底板堆叠形成结构与外观；
- 主控、电池和关键器件尽量可测试、可替换、可救砖；
- 优先满足夜间安静、左手盲操作、体积紧凑和低成本。

当前项目名和 PCB 丝印使用 `GALPANEL`。

## 2. 当前资料范围

项目目录目前包含：

- `GALGAME宏定义键盘设计.json`：一次完整的设计讨论导出，记录了从选型、布局、引脚到软件行为的演进过程；
- `PCB/V1.0`：V1.0 原理图和 PCB PDF；
- `PCB/V2.0`：V2.0 原理图和 PCB PDF；
- `PCB/V2.1`：V2.1 原理图和 PCB PDF；
- `SuperMini NRF52840 资料20230808`：nRF52840 产品规格、nice!nano Bootloader、J-Link、nRF Connect 和 Nordic 命令行工具资料。
- `config/boards/shields/galpanel`：当前初始化的 ZMK 硬件描述和 keymap。

当前目录仍没有发现：

- 嘉立创 EDA/KiCad 等可编辑工程源文件；
- Gerber、钻孔文件、坐标文件和正式生产 BOM；
- 外壳顶板/底板的独立工程文件；
- 自动化硬件测试记录。

2026-07-26 已完成第一次 GitHub Actions 编译和 UF2 刷写，Windows 能识别名为 `GALPANEL` 的键盘、媒体控制和鼠标复合 HID。本文对电气实现的判断仍以 V2.1 PDF 为准；当前 keymap 已通过编译与 USB 枚举验证，但机械键、侧键、EC11、BLE 和 LED 仍需逐项实测，不能视为最终量产固件。

## 3. 总体技术路线

```text
3.7 V 单节锂电池
        │
        ├─ 电源开关 ── ProMicro nRF52840 兼容模块
        │                 ├─ USB-C：有线 HID、供电、充电、UF2 烧录
        │                 ├─ BLE：无线 HID、多 Profile
        │                 └─ GPIO：按键、EC11、状态灯
        │
        └─ 不设置外置 ADC 电量检测

输入：3 颗 Choc V2 + 4 颗侧键 + SYS + RST + EC11 旋转/按压
输出：键盘按键 + 鼠标滚轮 + 音量媒体键 + 5 路 LED 位（4 正式、1 预留）
固件：ZMK / Zephyr，UF2 日常烧录，J-Link/SWD 作为救砖方案
```

主控选择的是低成本 ProMicro/nice!nano 形态的 nRF52840 兼容模块，而不是最初考虑的 XIAO nRF52840。选择原因是价格低、GPIO 较多、USB-C/BLE/电池充电能力齐全，并能复用 ZMK 的 ProMicro/nice!nano 生态。

## 4. 版本演进

|版本|主要结构|主要目的和变化|当前认知|
|---|---|---|---|
|V1.0|5 颗 Choc V2，2×3 风格布局，右上 EC11，主控位于板中/背面区域；3 个侧键、2 个板载键、2 个 LED、4 个安装孔|验证高功能密度、正反面器件共存、短排针安装和原始外形|已有实物；实测正反器件不冲突，短排针方案成立|
|V2.0|3 颗机械键横向排列；主控横放在上方，EC11 位于右上；3 个侧键、2 个板载键、2 个 LED、3 个安装孔|解决 V1 的布局拥挤问题，强化高频键一字排列和左右侧键的人体工学|结构方向成为后续基础版本|
|V2.1|沿用 V2.0 主体布局；增加第 4 个侧键、FN/WARN/AUX 灯、编码器 RC、LED 公共断开位，并恢复 4 个安装孔|形成最终输入数量、状态提示和 GPIO 分配|当前最新硬件版本，应作为后续固件和实测基准|

项目讨论的最终结论是不因 V1.0 机械上能容纳 5 键就恢复 5 键。3 颗正面机械键已经覆盖最高频操作，额外功能由侧键、EC11 和 Fn 层承担，盲操作、结构强度和外观都更清晰。

## 5. V2.1 硬件结构

### 5.1 输入器件

- 3 颗凯华 Choc V2/PG1353 类矮轴，直接焊接；
- 4 颗 TS4548B3J 侧按微动；
- 1 颗 ZX-QC36-4.3TPWD 系统键；
- 1 颗 ZX-QC36-4.3TPWD 硬件复位键；
- 1 颗 EC1106S/EC11 编码器，包含 A/B 两相信号和按压开关；
- 1 颗 SK12D07VG5 电源拨动开关。

普通按键均采用：

```text
GPIO -> 按键 -> GND
```

固件使用内部上拉，按下为低电平。项目没有使用按键矩阵和每键二极管，原因是输入数量少，GPIO 足够，直接连接更简单。

### 5.2 编码器滤波

V2.1 PDF 中，EC11 两相信号实际增加了：

```text
D21 -- R6 1 kΩ -- ENC_A，节点通过 C2 100 nF 接 GND
D20 -- R7 1 kΩ -- ENC_B，节点通过 C3 100 nF 接 GND
```

这是已经落入原理图和 PCB 的 RC 滤波，不再是单纯的软件消抖。后续应通过快速旋转、慢速逐格旋转和反向旋转测试确认是否漏计数或方向抖动。

### 5.3 状态灯

V2.1 设置 5 个 LED 位：

|名称|计划颜色|用途|装配状态|
|---|---|---|---|
|`LED_LINK`|蓝|BLE 配对、连接、重连和 Profile|正式|
|`LED_INFO`|绿|USB 输出、QSAVE/QLOAD、普通操作反馈|正式|
|`LED_FN`|白|Fn 层是否开启|正式|
|`LED_WARN`|红|错误、危险操作、清除配对确认|正式|
|`LED_AUX`|黄色或任意|未来 SYS 或其他提示|默认 DNP|

LED 为 GPIO 高电平点亮，所有阴极汇入独立的 `LED_GND`，再经 `R_LED_EN` 0 Ω 接系统 GND。拆除 `R_LED_EN` 后可统一断开全部 LED，用于测量无灯功耗。

V2.1 PDF 中 5 路限流电阻均为 2.2 kΩ。它与讨论中“不同颜色使用不同阻值、Fn 灯更暗”的建议并不完全一致，实际装配前应根据 LED 颜色、顶板透光结构和夜间亮度实测决定是否改值或用 PWM 限亮。

### 5.4 电池与供电

电池为带保护板的单节 3.7 V 锂电池，主控兼容模块承担充电管理。V2.1 电池正极路径为：

```text
BAT+ -> TP1 -> R4 0 Ω -> TP2 -> SW1 -> 主控 B+
```

电池负极接主控 `B-`。其预期行为是：

- 开关闭合：可由电池供电，插 USB 时可充电；
- 开关断开：电池与主控断开，不能给电池充电；
- 开关断开但插入 USB：仍可作为纯有线设备使用。

最终方案已经取消外置 ADC 电量检测，不使用 P1.01、P1.02、P1.07。因此当前硬件不能依靠载板分压电路精确测量电池电压；是否能报告电量取决于实际主控模块是否提供可用的板载检测能力。

## 6. V2.1 最终 GPIO 与网络认知

以下映射同时与最终讨论和 V2.1 原理图相符：

|ProMicro 引脚|nRF52840 GPIO|网络/器件|功能|
|---|---|---|---|
|D0|P0.08|`LED_AUX`|预留 LED，默认 DNP|
|D1|P0.06|`LED_WARN`|红色警告灯|
|D2|P0.17|`KEY_FAST`|左机械键|
|D3|P0.20|`KEY_AUTO`|中机械键|
|D4|P0.22|`KEY_NEXT`|右机械键|
|D5|P0.24|`KEY_QSAVE`|右上侧键|
|D6|P1.00|`LED_LINK`|蓝色连接灯|
|D7|P0.11|`KEY_DESKTOP`|左下侧键|
|D8|P1.04|`KEY_SYS`|板载系统键|
|D9|P1.06|`LED_INFO`|绿色信息灯|
|D14|P1.11|`KEY_FN`|右下侧键|
|D15|P1.13|`LED_FN`|白色 Fn 灯|
|D18|P1.15|`KEY_QLOAD`|左上侧键|
|D19|P0.02|`ENC_SW`|EC11 按压|
|D20|P0.29|`ENC_B`|EC11 B 相|
|D21|P0.31|`ENC_A`|EC11 A 相|
|RST|-|`RST`|隐藏式硬件复位|

明确不使用：

- D10/P0.09；
- D16/P0.10；
- 未引出的 P1.01、P1.02、P1.07。

D0、D1 被当作普通 GPIO 使用，固件不应同时启用占用它们的串口功能。

## 7. 计划的软件行为

### 7.1 基础层

|物理输入|输出|
|---|---|
|左机械键|按住 `Ctrl`，快进|
|中机械键|`A`，自动播放|
|右机械键|`Enter`，推进/确认|
|EC11 顺时针|鼠标滚轮下滚|
|EC11 逆时针|鼠标滚轮上滚|
|EC11 按压|`Esc`|

### 7.2 Fn 层

只有 3 颗机械键和 EC11 随 Fn 层改变：

|物理输入|Fn 层输出|
|---|---|
|左机械键|`S`，进入存档界面|
|中机械键|`R`，打开对话记录|
|右机械键|`L`，进入读档界面|
|EC11 顺时针|音量增加|
|EC11 逆时针|音量降低|
|EC11 按压|静音/恢复|

Fn 计划采用单击锁定/再次单击退出，启动后默认回到基础层；`LED_FN` 应反映真实层状态，而不是简单按键翻转。讨论中还建议 Fn 长时间无操作后自动退出，但该行为尚未形成代码。

### 7.3 固定侧键

四颗侧键不随 Fn 改变：

|位置|操作|功能|
|---|---|---|
|左上|双击|`F12`，QLOAD|
|左下|双击|`Win+D`，显示/隐藏桌面|
|右上|双击|`F11`，QSAVE|
|右下|单击|切换 Fn 层|

双击方案用于避免误触，目标窗口约 280～320 ms。单击 QLOAD/QSAVE/桌面键应不执行，三击应至多触发一次。

### 7.4 SYS 与 RST

SYS 的计划功能为：

- 单击：切换下一个 BLE Profile；
- 双击：切换 USB/BLE 输出；
- 长按约 2 秒：显示当前连接/Profile 状态；
- 长按约 6 秒：清除当前蓝牙 Profile 配对，并用警告灯确认。

RST 直接连接硬件复位脚，只负责复位/进入 Bootloader，不属于普通可编程按键。为了避免误触，最终结构应让它隐藏或需要工具按压。

上述快捷键不是所有 GALGAME 的统一标准。后续固件应允许按具体游戏/引擎调整 F11、F12、A、S、R、L 等映射，最好通过不同配置或 Profile 管理。

## 8. 固件和调试路线

2026-07-26 已先对实物开发板进行只读识别。开发板插入电脑后挂载为 `E:\`、卷标 `NICENANO`，其 `INFO_UF2.TXT` 显示：

```text
UF2 Bootloader 0.6.0
Model: nice!nano
Board-ID: nRF52840-nicenano
SoftDevice: S140 version 6.1.1
Date: Jun 19 2021
```

这使 nice!nano 兼容路线从“根据商品资料推断”变为“已经由实物 Bootloader 信息确认”。随后 GitHub Actions Run #1 使用 `nice_nano//zmk` 和 `galpanel` 编译成功，并将 `galpanel.uf2` 刷入实物。

刷写后的 Windows 实测结果：

- `NICENANO` 在复制结束后自动消失，Bootloader 正常跳转到应用；
- USB 总设备上报名称为 `GALPANEL`；
- VID/PID 为 `1D50:615E`；
- 同时枚举 Keyboard、Consumer Control 和 Mouse HID；
- ZMK 上游已固定到 `f84ef436cd8e88f9d1580a9b86bab2fae8d349c6`，避免后续构建随 `main` 漂移。

推荐路线是：

- ZMK + Zephyr，而不是 STM32 CubeMX/Keil 式裸机工程；
- 优先按实际模块的 Bootloader 尝试 `nice_nano//zmk` 板级目标；
- VS Code + WSL2/本地 Zephyr 工具链或 GitHub Actions 编译；
- 双击 RST 进入 UF2 Bootloader，拖入 UF2 完成日常烧录；
- J-Link + SWD 用于 Bootloader 损坏、全片擦除和底层调试。

项目希望设备在 Windows 中作为同一个复合 HID，同时上报：

- Keyboard HID；
- Mouse HID 滚轮；
- Consumer Control 音量键。

首次从纯键盘固件改为带鼠标能力的固件时，Windows/BLE 主机可能缓存旧 HID 描述，需要删除旧配对并重新配对一次；这与日常按 Fn 无关。

## 9. 机械与外壳认知

- 外壳采用主 PCB、顶板/外围框、底板和 M2 铜柱/尼龙柱堆叠；
- Choc V2 选择静音线性“孤岛 mini”一类轴体，约 43 gf，适合频繁推进和夜间使用；
- 主控通过短排针/低矮插座安装，最好可拔插；
- USB-C 应靠板边并获得机械支撑；
- 靠近天线的位置应使用尼龙螺丝或尼龙柱，避免大块金属；
- 安装孔优先为 M2、约 2.2 mm、NPTH、无铜环；
- 侧键应通过不同突出量形成盲操作区别，Fn 最容易触及，QLOAD 和桌面键更内缩。

V1.0 实测数据：

|项目|距主 PCB 顶面|
|---|---:|
|主控最高点|约 9 mm（原记录写 0.9 mm，结合结构按 0.9 cm 理解，仍需复测）|
|机械键帽最高点|约 11 mm|
|EC11 带旋钮最高点|约 21 mm|

主控比键帽低约 2 mm，说明低矮安装有效。EC11 比键帽高约 10 mm，当前偏高；建议最终旋钮顶面控制在 15～17 mm，即高出键帽约 4～6 mm。若暂时保留 21 mm，应使用轻质旋钮并在 EC11 下方增加非金属支撑，减小按压和侧碰产生的杠杆力。

## 10. 当前已知问题和不一致

### 10.1 V2.1 PDF 与最新丝印存在版本差异

V2.1 PCB 正面仍能看到：

```text
GALPANEL V2.0 DESIGNED BY REN_HUST
```

用户已在可编辑工程中更新丝印；当前目录中的 PDF 仍是更新前导出件。下次导出生产资料时应确认 PDF、Gerber 和实板均显示 V2.1。

### 10.2 LED BOM 与颜色意图不完全一致

V2.1 BOM 把 `AUX`、`FN`、`WARN` 三颗都列为 `LED_0603-R`，而设计意图分别是预留黄色、白色、红色。用户决定不再修改 EDA 中的 Comment，实际焊接时按 `LINK/INFO/FN/WARN/AUX` 功能位置选择对应颜色。因此装配清单必须另行明确颜色，不能让贴片厂仅按当前 BOM 自动代料。

### 10.3 LED 电阻全部为 2.2 kΩ

当前原理图/BOM 的 LED 电阻均为 2.2 kΩ。软件可使用 PWM 或占空控制进一步调暗，但限流电阻仍决定点亮瞬间的峰值电流和最大亮度。用户可以在装配时改焊 4.7 kΩ；最终应通过不同颜色 LED、顶板透光结构和夜间观感决定 2.2 kΩ/4.7 kΩ 的实际组合。

### 10.4 编码器 RC 参数与早期建议不同

早期讨论倾向 10 nF DNP，V2.1 实际使用 1 kΩ + 100 nF 且进入 BOM。该参数不必先验否定，但必须在实物上验证快速旋转是否丢步。

### 10.5 SWD 救砖接口未在 PDF 中明确落板

载板 PDF 中没有独立 SWD 测试点，但用户确认 SuperMini 模块本身带 SWD 接口，官方资料也提供 J-Link、nRF Connect Programmer、命令行工具和 Bootloader 镜像。只要模块 SWD 焊盘在装配后仍可接触，或模块保持可拔插，就已经具备救砖路径，不强制在载板重复布置 SWD。

### 10.6 天线环境仍需实测

ProMicro 模块天线端靠近 EC11、旋钮和板内金属环境。应确保天线下方及前方没有上下层铜、走线、过孔、电池线和金属柱，顶板/底板也不能在相应位置使用铜或金属装饰。最终必须比较裸板、装壳、手靠近旋钮、金属/塑料旋钮时的 BLE 稳定性。

### 10.7 没有外置 ADC 电量检测

这是当前阶段主动简化的结果，不是遗漏。它不影响充电芯片在充满后停止充电，但会限制电量百分比和低电量警告能力。ADC 被明确放到后续可选升级，不进入首版软件和硬件验收范围。

### 10.8 软件已完成首次 USB 闭环，进入裸板 BLE 验证

当前仓库已经建立 ZMK shield、设备树、基础/Fn keymap、双击行为、EC11、鼠标滚轮和 GitHub Actions 构建结构。云端编译、UF2 刷写、Windows 复合 HID 枚举和 D2/P0.17 的 Ctrl 输入已经通过。载板尚未焊接，因此其余 GPIO 暂缓完整验收，当前转入不依赖载板的 BLE 广播、配对、绑定和自动重连验证。LED 动画、SYS 长按、Fn 自动退出等仍需后续事件监听模块实现。

## 11. 当前项目状态判断

硬件方案已经完成从“概念选型”到“V2.1 可制造原理图/PCB”的收敛：

- 主控、输入数量、灯数和 GPIO 已经明确；
- V1.0 已验证主控短排针安装与正反面机械兼容；
- V2.0/V2.1 已把布局收敛为 3 键横排、上方主控和右上旋钮；
- V2.1 已增加四侧键、四正式灯、一预留灯、LED 总断开和编码器 RC。

但项目还没有完成“成品闭环”。当前更准确的阶段是：

> 硬件设计基本定版，ZMK 固件已完成 USB 首次闭环；当前利用裸开发板先验证 BLE，载板焊接后再完成全部 GPIO、EC11、LED 和整机联调。

## 12. 后续建议顺序

1. 在可编辑 EDA 工程中核对 V2.1 网络表、LED 极性、EC11 A/B/C、侧键 COM/NO、电源开关方向和全部 NC 引脚；
2. 重新导出已更新丝印的 V2.1 资料，并在装配说明中单独标明 LED 颜色与可选阻值；
3. 确认模块 SWD 焊盘在装配后可访问，或保持主控可拔插；
4. 复核天线全层净空、附近安装柱材质和电池/旋钮位置；
5. 用实物测量 EC11 高度并优化至 15～17 mm，或增加可靠支撑；
6. 使用现有 ZMK 固件完成 BLE 配对、纯无线输入和自动重连，再依次验证 EC11、鼠标滚轮、媒体键、Fn、双击侧键和 LED；
7. 完成 GPIO、USB、BLE、充电、功耗、按键消抖、编码器快速旋转和 RF 装壳测试；
8. 测试通过后再导出 Gerber、BOM、坐标文件和正式装配说明。

## 13. 最小验收清单

- 3 颗机械键、4 颗侧键、SYS、RST、EC11 全部方向和键值正确；
- Fn 状态与白灯始终同步，重启后回基础层；
- QLOAD/QSAVE/Win+D 不会被单击误触；
- USB 和 BLE 都可作为键盘使用，滚轮和音量功能同时存在；
- BLE 可配对、重连、切换 Profile，修改 HID 描述后可重新配对；
- 电池供电、USB 供电、开关断开、插线充电行为符合设计；
- EC11 快速旋转不漏步、不反跳；
- 裸板与装壳后的 BLE 稳定性无明显劣化；
- USB 插拔和按压旋钮时主 PCB 不明显弯曲；
- LED 亮度适合夜间使用，拆除 `R_LED_EN` 后所有灯完全断开；
- UF2 可正常进入，并具备可行的 SWD 救砖路径。
